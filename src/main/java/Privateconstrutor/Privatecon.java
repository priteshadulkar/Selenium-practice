package Privateconstrutor;

import Parentchild.Parent;
import Parentchild.child;

public class Privatecon {

 
   // 	child c = new child();    it call both parent and child class with normal constructor
	  // Parent p = new child();   it call both parent and child class with normal constructor same 
	//Parent p1 = new Parent();     it call only parent class 
}
